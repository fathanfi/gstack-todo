@AGENTS.md

## Testing

- Run tests with `npm run test`
- Test files live under `src/**/*.test.ts` and `src/**/*.test.tsx`
- See `TESTING.md` for conventions and coverage expectations
- Add tests for new behavior, bug fixes, and branch/error paths before shipping
